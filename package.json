{
  "name": "open-data",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:schema": "tsc && node dist/scripts/generate-schemas.js",
    "build:api": "tsc && node dist/scripts/generate-api.js",
    "validate:json": "tsc && node dist/scripts/validate-json.js",
    "build:exchange-rates": "tsc && node dist/scripts/generate-exchange-rates.js"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.47.6",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fs-extra": "^11.2.0",
    "glob": "^10.3.10",
    "quicktype-core": "^23.2.6",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4"
  }
}
